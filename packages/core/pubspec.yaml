name: core
description: Foundation package - networking, storage, env, base classes
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0
  get_it: ^8.0.3
  injectable: ^2.5.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.0
  logger: ^2.5.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.9.4
  injectable_generator: ^2.7.0
  mocktail: ^1.0.4
